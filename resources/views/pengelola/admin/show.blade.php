@extends('pengelola.layouts.app')
@section('title','Detail Admin - DonasiKita')
@section('page_title','Detail Admin')
@section('content')<div class="grid grid-cols-1 xl:grid-cols-[1fr_320px] gap-6"><div class="bg-white rounded-3xl border border-slate-100 shadow-sm p-6"><div class="flex items-start justify-between"><div><h2 class="font-display text-3xl font-extrabold">{{ $admin->nama }}</h2><p class="mt-2 text-slate-500">{{ $admin->email }} ? {{ $admin->no_hp ?? '-' }}</p></div><x-pengelola-status :status="$admin->status" /></div><div class="mt-6 rounded-2xl bg-slate-50 p-4"><p class="text-xs text-slate-500">Role</p><p class="font-bold">{{ $admin->role }}</p></div><h3 class="font-bold mt-8 mb-3">Kampanye dibuat</h3><ul class="space-y-2">@forelse($admin->kampanye as $kampanye)<li class="rounded-2xl border border-slate-100 p-3">{{ $kampanye->judul_kampanye }}</li>@empty<li class="text-slate-500">Belum membuat kampanye.</li>@endforelse</ul></div><div class="space-y-3"><a href="{{ route('pengelola.admin.edit',$admin) }}" class="block text-center rounded-2xl bg-amber-500 text-white font-bold px-5 py-3">Edit</a><form method="POST" action="{{ route('pengelola.admin.destroy',$admin) }}" onsubmit="return confirm('Hapus akun admin ini?')">@csrf @method('DELETE')<button class="w-full rounded-2xl bg-red-600 text-white font-bold px-5 py-3">Hapus</button></form><a href="{{ route('pengelola.admin.index') }}" class="block text-center rounded-2xl bg-slate-100 text-slate-700 font-bold px-5 py-3">Kembali</a></div></div>@endsection
