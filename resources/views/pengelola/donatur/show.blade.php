@extends('pengelola.layouts.app')
@section('title','Detail Donatur - DonasiKita')
@section('page_title','Detail Donatur')
@section('content')<div class="grid grid-cols-1 xl:grid-cols-[1fr_320px] gap-6"><div class="bg-white rounded-3xl border border-slate-100 shadow-sm p-6"><h2 class="font-display text-3xl font-extrabold">{{ $donatur->nama }}</h2><p class="mt-2 text-slate-500">{{ $donatur->email }} ? {{ $donatur->no_hp ?? '-' }}</p><div class="mt-6 rounded-2xl bg-slate-50 p-4"><p class="text-xs text-slate-500">Alamat</p><p class="font-semibold">{{ $donatur->alamat ?? '-' }}</p></div><h3 class="font-bold mt-8 mb-3">Riwayat transaksi</h3><div class="space-y-2">@forelse($donatur->donasi as $donasi)<a class="block rounded-2xl border border-slate-100 p-3 hover:bg-slate-50" href="{{ route('pengelola.donasi.show',$donasi) }}"><div class="flex justify-between gap-3"><span>{{ $donasi->kampanye->judul_kampanye ?? 'Kampanye' }}</span><strong>Rp {{ number_format($donasi->nominal,0,',','.') }}</strong></div><x-pengelola-status :status="$donasi->status_donasi" /></a>@empty<p class="text-slate-500">Belum ada transaksi.</p>@endforelse</div></div><div class="space-y-3"><a href="{{ route('pengelola.donatur.edit',$donatur) }}" class="block text-center rounded-2xl bg-amber-500 text-white font-bold px-5 py-3">Edit</a><form method="POST" action="{{ route('pengelola.donatur.destroy',$donatur) }}" onsubmit="return confirm('Hapus donatur ini?')">@csrf @method('DELETE')<button class="w-full rounded-2xl bg-red-600 text-white font-bold px-5 py-3">Hapus</button></form><a href="{{ route('pengelola.donatur.index') }}" class="block text-center rounded-2xl bg-slate-100 text-slate-700 font-bold px-5 py-3">Kembali</a></div></div>@endsection
