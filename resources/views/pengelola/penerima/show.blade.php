@extends('pengelola.layouts.app')
@section('title','Detail Penerima - DonasiKita')
@section('page_title','Detail Penerima')
@section('content')<div class="grid grid-cols-1 xl:grid-cols-[1fr_320px] gap-6"><div class="bg-white rounded-3xl border border-slate-100 shadow-sm p-6"><h2 class="font-display text-3xl font-extrabold">{{ $penerima->nama }}</h2><p class="mt-2 text-slate-500">{{ $penerima->kategori_penerima ?? 'Tanpa kategori' }} ? {{ $penerima->no_hp ?? '-' }}</p><div class="mt-6 rounded-2xl bg-slate-50 p-4"><p class="text-xs text-slate-500">Alamat</p><p class="font-semibold">{{ $penerima->alamat }}</p></div><div class="mt-4 whitespace-pre-line text-slate-700">{{ $penerima->deskripsi_kebutuhan ?? '-' }}</div><h3 class="font-bold mt-8 mb-3">Kampanye terkait</h3><ul class="space-y-2">@forelse($penerima->kampanye as $kampanye)<li class="rounded-2xl border border-slate-100 p-3">{{ $kampanye->judul_kampanye }}</li>@empty<li class="text-slate-500">Belum dipakai kampanye.</li>@endforelse</ul></div><div class="space-y-3"><a href="{{ route('pengelola.penerima.edit',$penerima) }}" class="block text-center rounded-2xl bg-amber-500 text-white font-bold px-5 py-3">Edit</a><form method="POST" action="{{ route('pengelola.penerima.destroy',$penerima) }}" onsubmit="return confirm('Hapus penerima ini?')">@csrf @method('DELETE')<button class="w-full rounded-2xl bg-red-600 text-white font-bold px-5 py-3">Hapus</button></form><a href="{{ route('pengelola.penerima.index') }}" class="block text-center rounded-2xl bg-slate-100 text-slate-700 font-bold px-5 py-3">Kembali</a></div></div>@endsection
